extends layout.pug

block content
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9
        h1.error_message= `${error.message} - Status: ${error.statusCode}`
        p= error.stack
        nav.grid-container.portfolio-breadcrumb
            a(href='/') ← Back            